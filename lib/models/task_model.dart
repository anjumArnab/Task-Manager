class Task {
  String title;
  String description;
  String timeAndDate;
  String priority;
  bool isChecked;

  Task({
    required this.title,
    required this.description,
    required this.timeAndDate,
    required this.priority,
    required this.isChecked,
  });
}
